import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  home: {
    justifyContent: 'center',
  },
  searchInput: {
    fontSize: 17,
    borderWidth: 1,
    borderColor: '#ccc',
    width: '100%',
    padding: 15,
    borderRadius: 30,
    marginBottom: 15,
  },
});
