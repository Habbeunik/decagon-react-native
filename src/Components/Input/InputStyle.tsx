import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    fontSize: 17,
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 15,
    borderRadius: 30,
  },
});
