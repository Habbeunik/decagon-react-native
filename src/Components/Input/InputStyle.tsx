import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    fontSize: 17,
    borderWidth: 1,
    borderColor: '#ccc',
    paddingHorizontal: 25,
    paddingVertical: 15,
    borderRadius: 30,
    fontFamily: 'WorkSans',
  },
});
