import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    padding: 15,
    borderRadius: 30,
    backgroundColor: '#00875a',
    width: '100%',
  },
  disabled: {
    backgroundColor: '#ccc',
  },
  label: {
    fontSize: 16,
    color: '#fff',
    textAlign: 'center',
    fontFamily: 'WorkSans',
  },
});
